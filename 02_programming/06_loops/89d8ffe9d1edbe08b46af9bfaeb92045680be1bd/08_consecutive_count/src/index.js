function consecutiveCount(string, letter) {
  // Code here
  for (let i = 0; i < string ; i ++){
string.includes((letter)=> {

}
  }
}

// Do not remove the following line, it is for tests
module.exports = consecutiveCount;
